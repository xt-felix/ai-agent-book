# 第 2 章：大模型基础 — Agent 的大脑

# 第二章 大模型基础 — Agent 的大脑

Agent 看起来像“会思考、会调用工具、会自主完成任务”的系统，但它真正的“大脑”，仍然是大语言模型（LLM）。

如果把 Agent 拆开看，通常会有三层：

- **大模型层**：负责理解用户意图、规划步骤、生成答案、决定是否调用工具
- **工具层**：负责执行真实操作，比如查数据库、发 HTTP 请求、写文件、调用搜索 API
- **运行时层**：负责状态管理、记忆、重试、权限控制、日志与监控

其中，**大模型层决定了 Agent 的上限**。  
模型是否擅长推理、是否稳定遵循指令、是否支持长上下文、是否原生支持工具调用，都会直接影响 Agent 的表现。

这一章不追求学术推导，而是从工程角度理解几个最关键的问题：

- Transformer 为什么能成为 Agent 的核心
- 上下文窗口为什么会直接影响 Agent 的任务边界
- 不同模型适合什么样的业务场景
- Temperature / Top-P / Max Tokens 如何改变 Agent 行为
- 为什么结构化输出是 Agent 工程化的基础设施
- 如何通过 Router 模式控制成本，在效果和预算之间取得平衡

---

## 2.1 Transformer 架构速览

今天主流的大模型，无论是 GPT、Claude、DeepSeek 还是 Qwen，底层都可以归到 Transformer 家族。

你不需要推导注意力公式，也不需要记住矩阵乘法的细节，但必须理解它是如何“读文本”和“做决策”的。

## 2.1.1 从“下一个词预测”到“Agent 决策”

大模型最基本的训练目标可以粗略理解为：

> 给定前面的文本，预测下一个最可能出现的 token。

这里的关键不是“背答案”，而是模型在海量文本中学到了：

- 语法结构
- 事实知识
- 代码模式
- 推理模板
- 指令遵循能力
- 工具调用格式

因此，当你给模型输入：

- 用户问题
- 系统提示词
- 工具定义
- 历史对话
- 环境状态
- 中间推理结果

模型会把这些内容一起看作“当前上下文”，然后继续生成下一个 token。  
而 Agent 所谓的“规划”“反思”“调用工具”，本质上都是这个生成过程的一部分。

---

## 2.1.2 Transformer 的最小理解框架

可以把 Transformer 想象成下面这个流水线：

```text
用户输入 / 系统提示 / 历史消息
        ↓
     Tokenization
        ↓
   Embedding（向量化）
        ↓
 多层 Transformer Block
   ├─ Self-Attention：看上下文里哪些部分更重要
   └─ Feed Forward：做进一步特征变换
        ↓
   预测下一个 Token 的概率分布
        ↓
  采样 / 解码（temperature, top-p）
        ↓
      输出文本 / JSON / tool call
```

更直观一点：

```text
┌─────────────────────────────────────┐
│ 输入上下文                          │
│ 系统提示 + 用户问题 + 工具定义 + 历史 │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│ Token 化                            │
│ “帮我查一下北京天气” → 一串 token      │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│ Self-Attention                      │
│ 每个 token 都会关注上下文中的其他 token │
│ 判断哪些信息与当前生成最相关          │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│ 多层堆叠                            │
│ 从词级别模式 → 句子级别 → 任务级别理解 │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│ 输出层                              │
│ 决定下一个 token / 是否触发工具调用   │
└─────────────────────────────────────┘
```

---

## 2.1.3 Self-Attention 为什么重要

如果说 RNN 更像“一边读一边记”，那 Transformer 更像“把整段上下文摊开看”。

Self-Attention 的工程意义在于：

- 模型可以直接关注长距离信息
- 更容易处理复杂指令
- 更擅长多轮对话中的引用关系
- 对代码、表格、长文档、工具定义更友好

例如下面的任务：

> “根据第 3 节合同条款和附录 B，判断供应商是否满足 SLA，并生成风险摘要。”

这个问题依赖跨段落、多位置的信息关联。  
Transformer 的强项，就是在生成结论时“回看”输入中的关键片段。

对 Agent 来说，这意味着它更适合做：

- 文档问答
- 长链路任务规划
- 多工具协同
- 代码生成与修改
- 跨文件分析

---

## 2.1.4 为什么说 Agent 是“提示词驱动的程序”

传统程序的控制逻辑写在代码里。  
Agent 则是“代码 + prompt + model”共同决定行为。

一个典型 Agent 请求里，实际输入给模型的内容可能是这样的：

```text
[system]
你是一个严谨的企业数据分析 Agent。
你必须优先调用工具获取真实数据，不能编造。
输出必须是 JSON。

[tools]
searchOrders(...)
queryWarehouse(...)
sendEmail(...)

[user]
帮我分析本周订单延迟的主要原因，并发给运营团队。

[history]
...
```

Transformer 会把它们统统当作上下文。  
也就是说，**Agent 的行为不是硬编码出来的，而是通过上下文“临时编程”出来的。**

这也是为什么：

- Prompt 设计会影响 Agent 的执行质量
- 工具描述写得不好，工具调用就会失败
- 历史上下文太乱，模型就容易偏航
- 结构化输出约束越强，结果越稳定

---

## 2.2 上下文窗口对 Agent 的意义

上下文窗口（Context Window）是 Agent 设计里最容易被低估、但实际影响极大的参数。

它决定了模型一次最多能“看到”多少 token。  
你可以把它理解成模型的**工作记忆容量**。

常见数量级：

- **32k / 64k**：适合普通对话、短文档分析
- **128k**：适合复杂业务 Agent，多轮对话 + 工具定义 +中等规模文档
- **1M**：适合大仓库代码分析、超长合同、长历史会话、批量资料汇总

---

## 2.2.1 上下文窗口装的到底是什么

很多人以为上下文窗口只装“用户说的话”，其实远不止。

一个 Agent 请求常常包含：

- 系统提示词
- 开发者提示词
- 用户当前输入
- 多轮历史消息
- 工具定义（JSON Schema）
- 检索召回的文档片段
- 记忆摘要
- 中间推理结果
- 失败重试时的错误日志
- 输出格式约束

比如一个企业知识库 Agent，单次请求就可能长这样：

| 组成部分 | 典型 token 消耗 |
|---|---:|
| 系统提示词 | 500 - 1500 |
| 工具定义 | 1000 - 4000 |
| 历史对话 | 2000 - 10000 |
| 检索文档片段 | 5000 - 40000 |
| 输出 schema | 300 - 1500 |
| 用户问题 | 50 - 500 |

所以你会发现，**真正吃掉上下文的，往往不是用户问题，而是 Agent 框架本身。**

---

## 2.2.2 128k 与 1M 的实际差别

### 场景一：代码仓库分析 Agent

假设你要做一个“代码审查 Agent”，需要理解：

- 30 个核心文件
- 每个文件 500~2000 行
- 还有 README、设计文档、测试日志

如果是 **128k**：

- 可以放下问题相关的少量文件
- 通常必须先做检索或摘要
- 更依赖外部 RAG 和分块策略
- 容易丢失跨文件依赖关系

如果是 **1M**：

- 可以一次性塞进更多源码和文档
- 更适合做全局架构理解
- 对“跨模块调用链分析”更友好
- 可以减少预处理和摘要损失

### 场景二：长对话客服 Agent

如果一个客服会话持续两三百轮：

- **128k** 通常要做历史压缩、摘要滚动
- **1M** 可以保留更完整的上下文，减少“遗忘用户偏好”的问题

### 场景三：法律/财务文档 Agent

面对几十万字合同、审计材料、招投标文件：

- **128k** 仍然需要检索式阅读
- **1M** 更接近“整包通读”的体验

---

## 2.2.3 大窗口不等于不需要 RAG

很多团队看到 1M 上下文后，会产生一个误解：

> 既然窗口这么大，是不是不需要检索增强了？

答案通常是：**仍然需要。**

原因有三个：

### 1）成本问题
窗口越大，输入 token 成本越高。  
把几十万 token 全部塞进去，价格可能远超预期。

### 2）注意力稀释
理论上模型能看见，不代表它一定“看得准”。  
无关内容太多时，关键信号会被淹没。

### 3）延迟问题
长上下文请求通常更慢。对线上 Agent 来说，响应时间非常关键。

因此更合理的做法是：

- **大窗口 + 检索**：优先召回最相关内容
- **必要时再扩窗**：只在复杂场景中使用超长上下文
- **结合摘要机制**：把长历史压缩成可消费的状态

---

## 2.2.4 Agent 设计中的上下文策略

工程上，推荐把上下文分成三层：

| 层级 | 内容 | 策略 |
|---|---|---|
| 固定上下文 | system prompt、工具定义、输出 schema | 尽量精简、复用缓存 |
| 动态上下文 | 当前用户输入、相关文档、环境状态 | 基于检索按需注入 |
| 长期上下文 | 历史对话、用户偏好、过去执行结果 | 做摘要、记忆提取 |

一个实用原则：

> 不是“能塞多少就塞多少”，而是“只给模型当前决策真正需要的信息”。

---

## 2.3 模型选型指南：GPT-5 / Claude Opus / DeepSeek-V4 / Qwen3

选模型不是“谁最强就用谁”，而是看你的任务目标：

- 要不要强推理
- 要不要长上下文
- 要不要低成本
- 要不要私有化部署
- 要不要稳定工具调用
- 要不要中英文混合能力
- 要不要代码能力

下面先给一个工程化视角的对比。

## 2.3.1 模型能力对比表

> 注意：具体价格、窗口、速率限制会随版本变化，下面侧重选型思路。

| 模型 | 优势 | 短板 | 适合场景 | 是否适合 Agent 核心决策 |
|---|---|---|---|---|
| GPT-5 | 指令遵循强、综合能力均衡、工具调用与结构化输出稳定 | 成本通常较高 | 通用企业 Agent、复杂工作流、代码与文档混合任务 | 非常适合 |
| Claude Opus | 长文理解强、总结归纳强、复杂文本处理稳定 | 某些强结构化场景需要更严格约束 | 法务、咨询、长报告分析、超长文档问答 | 非常适合 |
| DeepSeek-V4 | 性价比高、推理/代码能力突出、适合大规模调用 | 某些细粒度指令稳定性可能需要额外测试 | 编码 Agent、分析型 Agent、预算敏感业务 | 适合 |
| Qwen3 | 中文表现好、开源生态活跃、可私有化部署 | 商业闭源顶级模型在某些复杂任务上仍有优势 | 中文业务系统、内网 Agent、本地化部署 | 很适合特定场景 |

---

## 2.3.2 按场景选模型

### 1）企业内部知识助手
特点：

- 中文文档多
- 需要权限控制
- 成本敏感
- 有私有部署诉求

推荐：

- **Qwen3**：如果你希望私有化、控制数据出域风险
- **DeepSeek-V4**：如果你更看重性价比和代码/分析能力
- **GPT-5**：如果预算充足，追求整体稳定性和开发效率

### 2）代码生成 / 修复 Agent
看重：

- 多文件理解
- 补全质量
- 单元测试生成
- 错误修复能力

推荐：

- **GPT-5**：综合稳
- **DeepSeek-V4**：性价比很强，尤其适合高频开发场景
- **Qwen3**：适合中文团队内网研发助手

### 3）法务、财务、投研类长文档 Agent
看重：

- 长文本保持能力
- 归纳总结
- 细节一致性
- 风险点提取

推荐：

- **Claude Opus**
- **GPT-5**（若同时需要更强工具编排）

### 4）多步骤工作流 Agent
例如：

- 收集信息
- 生成计划
- 调用多个 API
- 汇总结果
- 输出结构化报告

推荐：

- **GPT-5**：工具调用与指令遵循通常更稳
- **DeepSeek-V4**：如果你有大量调用需求，希望控制预算

---

## 2.3.3 一个务实的选型原则

可以用这张决策表快速判断：

| 需求 | 优先考虑 |
|---|---|
| 最强综合能力 | GPT-5 |
| 超长文档理解 | Claude Opus |
| 高性价比大规模调用 | DeepSeek-V4 |
| 中文 + 私有化部署 | Qwen3 |

如果你不知道从哪开始，最稳妥的方式是：

1. 先选一个**强模型做 baseline**
2. 用真实任务集评估成功率、延迟、成本
3. 再尝试把一部分任务切给更便宜的模型

这就是后面会讲到的 **Router 模式**。

---

## 2.4 Temperature、Top-P、Max Tokens 如何影响 Agent 行为

很多 Agent “不稳定”，并不是 prompt 写错了，而是采样参数没配对。

这三个参数会直接影响模型的输出风格和行为边界。

---

## 2.4.1 Temperature：控制“发散程度”

`temperature` 可以理解成输出随机性的旋钮。

- **低温度（0 ~ 0.3）**：更稳定、更保守、更接近确定性
- **中温度（0.4 ~ 0.7）**：平衡稳定性和表达灵活性
- **高温度（0.8+）**：更有创造性，但更容易跑偏

对 Agent 来说：

| 任务类型 | 推荐 Temperature |
|---|---:|
| 工具调用决策 | 0 ~ 0.2 |
| JSON 结构化输出 | 0 ~ 0.2 |
| 数据抽取 | 0 ~ 0.3 |
| 代码生成 | 0.1 ~ 0.4 |
| 文案创作 | 0.7 ~ 1.0 |
| 头脑风暴 | 0.8 ~ 1.2 |

关键结论：

> **越需要稳定执行，temperature 越低。**

如果你的 Agent 要输出 JSON、选择工具、生成 SQL、调用 API，就不要用太高温度。

---

## 2.4.2 Top-P：控制候选词范围

`top_p` 是另一种采样控制方式，它会从累计概率达到某阈值的候选 token 中进行采样。

简单理解：

- **top_p 小**：候选词范围更窄，输出更保守
- **top_p 大**：候选词范围更宽，输出更自由

在工程上，一般遵循：

- 如果你已经调 `temperature`，通常不需要大幅折腾 `top_p`
- 生产环境常用 `top_p = 1`
- 只有当你明确需要收紧输出时，再降低 `top_p`

推荐经验值：

| 场景 | Temperature | Top-P |
|---|---:|---:|
| Tool calling / JSON | 0.1 | 1 |
| 提取摘要 | 0.2 | 0.9~1 |
| 一般问答 | 0.5 | 1 |
| 创意写作 | 0.9 | 0.95~1 |

---

## 2.4.3 Max Tokens：控制“能说多长”

`max_tokens` 限制模型最多生成多少 token。  
它影响的不只是成本，还有任务是否能完成。

如果设得太小，可能出现：

- JSON 输出被截断
- SQL 只写了一半
- 分析报告中途停止
- 工具调用参数不完整

如果设得太大，可能出现：

- 成本上升
- 响应时间变长
- 模型过度啰嗦
- 在某些场景中“想太多”

推荐策略：

| 任务 | 建议 max_tokens |
|---|---:|
| 分类 / 路由 | 50 ~ 200 |
| 简短 JSON | 200 ~ 800 |
| 工具调用参数生成 | 200 ~ 1000 |
| 中等分析报告 | 1000 ~ 3000 |
| 长报告 / 多步总结 | 3000+ |

经验法则：

> 输出是结构化的，就按 schema 预估上限；输出是自然语言的，就按业务允许的最大长度限制。

---

## 2.4.4 配置示例：TypeScript

下面是一个实际可运行的配置示例，演示不同任务用不同参数。

```ts
type LLMConfig = {
  model: string;
  temperature: number;
  top_p: number;
  max_tokens: number;
};

const presets: Record<string, LLMConfig> = {
  tool_calling: {
    model: "gpt-5",
    temperature: 0.1,
    top_p: 1,
    max_tokens: 600,
  },
  json_extraction: {
    model: "gpt-5",
    temperature: 0.0,
    top_p: 1,
    max_tokens: 800,
  },
  code_generation: {
    model: "deepseek-v4",
    temperature: 0.2,
    top_p: 1,
    max_tokens: 2000,
  },
  creative_writing: {
    model: "claude-opus",
    temperature: 0.9,
    top_p: 0.95,
    max_tokens: 3000,
  },
};

function getPreset(task: keyof typeof presets): LLMConfig {
  return presets[task];
}

console.log(getPreset("tool_calling"));
```

---

## 2.5 结构化输出为什么对 Agent 至关重要

如果说 Prompt 让模型“更聪明”，那么**结构化输出**让 Agent “真正可用”。

自然语言适合给人读，但对程序来说不稳定。  
你很难放心地把下面这种回答直接喂给后续系统：

> 我觉得应该先调用天气接口，然后顺便查一下未来三天的降雨概率，城市可能是北京。

程序需要的是：

```json
{
  "tool": "get_weather_forecast",
  "arguments": {
    "city": "北京",
    "days": 3
  }
}
```

这就是结构化输出的价值。

---

## 2.5.1 没有结构化输出会发生什么

常见问题包括：

- 字段名不一致
- JSON 缺逗号、缺引号
- 漏填必填字段
- 输出夹杂解释文字
- 工具参数类型错误
- 多个步骤混在一段自然语言里

这些问题在人看起来“小毛病”，但对系统来说就是执行失败。

Agent 一旦需要：

- 调工具
- 调工作流
- 写数据库
- 返回给前端渲染
- 与其他服务联动

就必须使用结构化输出。

---

## 2.5.2 JSON Mode 与 Tool Use 的区别

两者都很重要，但用途略有不同。

| 方式 | 适合场景 | 优点 | 注意点 |
|---|---|---|---|
| JSON Mode | 提取、分类、规划、生成固定结构结果 | 输出规整，便于程序解析 | 仍需 schema 校验 |
| Tool Use | 需要模型决定是否调用函数/工具 | 天然适合 Agent 执行动作 | 工具描述必须清晰 |

### JSON Mode
模型直接返回 JSON，例如：

```json
{
  "intent": "book_flight",
  "destination": "上海",
  "date": "2026-05-08"
}
```

适合：

- 意图识别
- 信息抽取
- 任务规划
- 评分与分类
- 前端组件数据生成

### Tool Use
模型不只是“说答案”，而是选择调用某个工具：

```json
{
  "name": "search_flights",
  "arguments": {
    "from": "北京",
    "to": "上海",
    "date": "2026-05-08"
  }
}
```

适合：

- 搜索
- 查询数据库
- 发消息
- 下工单
- 执行真实动作

---

## 2.5.3 TypeScript：用 Zod 保证输出可靠

仅仅让模型“输出 JSON”还不够，必须做校验。  
在 TypeScript 里，最常见的做法是配合 `zod`。

```ts
import { z } from "zod";

const TaskPlanSchema = z.object({
  goal: z.string(),
  steps: z.array(
    z.object({
      id: z.string(),
      action: z.string(),
      tool: z.string().nullable(),
    })
  ),
  risk_level: z.enum(["low", "medium", "high"]),
});

type TaskPlan = z.infer<typeof TaskPlanSchema>;

const raw = {
  goal: "分析订单延迟原因并通知运营",
  steps: [
    { id: "1", action: "查询订单数据", tool: "query_orders" },
    { id: "2", action: "统计延迟原因", tool: "analyze_delays" },
    { id: "3", action: "发送邮件", tool: "send_email" },
  ],
  risk_level: "medium",
};

const parsed: TaskPlan = TaskPlanSchema.parse(raw);
console.log(parsed.steps[0].action);
```

安装依赖：

```bash
npm install zod
```

这段代码可直接运行。它的意义在于：

- 模型负责生成候选结果
- `zod` 负责做强校验
- 校验失败就重试或降级处理

这才是生产级 Agent 的姿势。

---

## 2.5.4 Python：Pydantic 校验示例

如果你的工具链在 Python，可以用 `pydantic`：

```python
from pydantic import BaseModel
from typing import List, Optional, Literal

class Step(BaseModel):
    id: str
    action: str
    tool: Optional[str]

class TaskPlan(BaseModel):
    goal: str
    steps: List[Step]
    risk_level: Literal["low", "medium", "high"]

raw = {
    "goal": "分析订单延迟原因并通知运营",
    "steps": [
        {"id": "1", "action": "查询订单数据", "tool": "query_orders"},
        {"id": "2", "action": "统计延迟原因", "tool": "analyze_delays"},
        {"id": "3", "action": "发送邮件", "tool": "send_email"},
    ],
    "risk_level": "medium"
}

plan = TaskPlan.model_validate(raw)
print(plan.steps[1].action)
```

安装：

```bash
pip install pydantic
```

---

## 2.6 成本优化：何时用大模型，何时用小模型

如果你把所有请求都打到最强模型上，效果通常不会太差，但账单会很吓人。

Agent 系统真正落地后，经常会面临三个现实约束：

- **请求量大**
- **延迟敏感**
- **预算有限**

所以必须学会一个核心模式：**Router（路由）模式**。

---

## 2.6.1 Router 模式是什么

Router 的思路很简单：

> 先判断任务难度，再把请求分配给最合适的模型。

不是所有任务都值得用最贵的模型。  
例如：

- 简单分类、格式转换、摘要压缩 → 小模型足够
- 多步推理、复杂工具调用、关键决策 → 大模型更稳

典型分层如下：

| 层级 | 任务 | 推荐模型策略 |
|---|---|---|
| L1 | 分类、改写、关键词提取、短摘要 | 小模型 |
| L2 | 普通问答、轻度工具调用、单文档分析 | 中模型 |
| L3 | 多步规划、复杂推理、多工具协同 | 大模型 |
| L4 | 高风险关键决策、失败升级重试 | 顶级模型 |

---

## 2.6.2 哪些任务适合小模型

小模型适合：

- 意图分类
- 文本清洗
- 标签抽取
- 句子改写
- 摘要压缩
- FAQ 问答
- 低风险代码补全

这些任务的特点是：

- 输出空间小
- 对推理深度要求低
- 即使偶尔出错，也能容忍或重试

---

## 2.6.3 哪些任务必须上大模型

以下任务通常更值得用大模型：

- 多步骤任务规划
- 复杂代码修复
- 跨文档证据整合
- 高风险业务决策
- 多工具链协同执行
- 对结果稳定性要求极高的场景

例如：

- “帮我把这个仓库升级到新版框架并修复测试”
- “从 20 份合同中识别付款风险并生成谈判建议”
- “分析过去 6 个月供应链波动并给出补货策略”

这类任务不是简单文本生成，而是“近似决策系统”，模型能力差异会被急剧放大。

---

## 2.6.4 Router 配置示例

下面用 TypeScript 写一个简单可运行的模型路由器。

```ts
type TaskType =
  | "classify"
  | "rewrite"
  | "extract"
  | "qa"
  | "tool_call"
  | "planning"
  | "complex_reasoning";

type RouteResult = {
  model: string;
  temperature: number;
  max_tokens: number;
};

function routeTask(taskType: TaskType, riskLevel: "low" | "medium" | "high"): RouteResult {
  if (riskLevel === "high") {
    return {
      model: "gpt-5",
      temperature: 0.1,
      max_tokens: 2000,
    };
  }

  switch (taskType) {
    case "classify":
    case "rewrite":
    case "extract":
      return {
        model: "qwen3",
        temperature: 0,
        max_tokens: 300,
      };
    case "qa":
    case "tool_call":
      return {
        model: "deepseek-v4",
        temperature: 0.2,
        max_tokens: 1200,
      };
    case "planning":
    case "complex_reasoning":
      return {
        model: "gpt-5",
        temperature: 0.1,
        max_tokens: 2500,
      };
    default:
      return {
        model: "deepseek-v4",
        temperature: 0.2,
        max_tokens: 1000,
      };
  }
}

console.log(routeTask("classify", "low"));
console.log(routeTask("planning", "medium"));
console.log(routeTask("tool_call", "high"));
```

这个版本虽然简单，但已经体现了生产设计中的核心思想：

- **按任务类型路由**
- **按风险等级升级**
- **不同模型使用不同参数模板**

---

## 2.6.5 更实用的两段式路由

在真实系统里，更推荐这样做：

### 第一段：轻量路由
先用便宜模型或规则系统判断：

- 任务类型
- 风险等级
- 是否需要工具
- 是否涉及长文档
- 是否需要升级大模型

### 第二段：主模型执行
根据第一段结果，选择：

- 小模型直接回答
- 中模型调用工具
- 大模型执行复杂规划
- 顶级模型兜底重试

这种模式有两个好处：

1. **显著省钱**
2. **延迟更可控**

---

## 2.6.6 一个务实的成本控制策略

建议把 Agent 请求分成三类预算桶：

| 桶 | 请求类型 | 策略 |
|---|---|---|
| 低成本桶 | 高频、低风险任务 | 默认小模型 |
| 中成本桶 | 常规业务任务 | 中模型优先，必要时升级 |
| 高成本桶 | 关键任务、失败重试、人工替代成本高 | 直接大模型 |

一个非常实用的经验：

> 如果模型调用成本低于人工处理成本的 1/10，通常就是值得的。  
> 如果大模型调用只是为了把“简单任务做得更豪华”，往往是在浪费预算。

---

## 2.7 配置模板：适合 Agent 的模型参数基线

最后给出一组可以直接落地的配置基线，作为你做 Agent 的默认起点。

| 用途 | 模型建议 | temperature | top_p | max_tokens | 备注 |
|---|---|---:|---:|---:|---|
| 意图分类 | Qwen3 / 小模型 | 0.0 | 1.0 | 100 | 追求稳定 |
| 信息抽取 | Qwen3 / DeepSeek-V4 | 0.0 ~ 0.2 | 1.0 | 300~800 | 搭配 schema 校验 |
| 工具调用决策 | GPT-5 / DeepSeek-V4 | 0.0 ~ 0.2 | 1.0 | 500~1200 | 避免高温度 |
| 多步规划 | GPT-5 | 0.1 ~ 0.3 | 1.0 | 1500~3000 | 复杂任务核心 |
| 长文总结 | Claude Opus / GPT-5 | 0.2 ~ 0.5 | 0.9~1.0 | 2000+ | 注意上下文成本 |
| 创意生成 | Claude Opus / GPT-5 | 0.8 ~ 1.0 | 0.95~1.0 | 1500+ | 不适合作为执行 Agent 核心 |

可以把它们写成统一配置文件：

```ts
export const agentModelProfiles = {
  classifier: {
    model: "qwen3",
    temperature: 0.0,
    top_p: 1.0,
    max_tokens: 100,
  },
  extractor: {
    model: "deepseek-v4",
    temperature: 0.1,
    top_p: 1.0,
    max_tokens: 600,
  },
  toolCaller: {
    model: "gpt-5",
    temperature: 0.1,
    top_p: 1.0,
    max_tokens: 1000,
  },
  planner: {
    model: "gpt-5",
    temperature: 0.2,
    top_p: 1.0,
    max_tokens: 2500,
  },
  summarizer: {
    model: "claude-opus",
    temperature: 0.3,
    top_p: 0.95,
    max_tokens: 2200,
  },
} as const;
```

---

大模型并不是 Agent 的全部，但它确实是 Agent 的思考核心。  
理解 Transformer，让你知道模型为什么能“读懂上下文”；理解上下文窗口，让你知道 Agent 的任务边界在哪里；理解模型选型和采样参数，让你知道如何把模型调到“既能干活，又不失控”；理解结构化输出和 Router 模式，则意味着你开始从“会调用 API”进入“能做生产系统”。

下一步，我们会进入更具体的 Agent 运行方式：它如何规划、如何记忆、如何调用工具、如何在失败后恢复。届时你会看到，真正让 Agent 落地的，不只是模型有多强，而是整个执行闭环设计得有多稳。
